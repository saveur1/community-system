import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rich_rwanda.community_listening',
  appName: 'Community Listening',
  webDir: 'public_html'
};

export default config;
